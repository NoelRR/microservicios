server:
  port: 8761

spring:
  application:
    name: eureka-server

# Eureka server no se registra a si mismo ni busca peers
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
