server:
  port: 8082

spring:
  application:
    name: menu-service
  datasource:
    url: jdbc:postgresql://localhost:5433/menudb
    username: toby
    password: toby
  jpa:
    hibernate:
      ddl-auto: update   # crea/actualiza tablas automaticamente (dev)
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: toby
    password: toby

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
