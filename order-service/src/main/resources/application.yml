server:
  port: 8083

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5433/orderdb
    username: toby
    password: toby
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: toby
    password: toby

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
